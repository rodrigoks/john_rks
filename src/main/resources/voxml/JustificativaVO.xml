<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="justificativa">

    <insert id="insert" parameterType="JustificativaVO" useGeneratedKeys="true" keyProperty="id" >

        INSERT INTO logbook19_justificativas (
            flogbook19_descricao,
            flogbook19_responsavel,
            flogbook19_data_cadastro,
            flogbook18_id
        ) VALUES (
        	#{descricao},
        	#{responsavel},
        	#{dataCadastro},
            #{grupo.id}
        )

    </insert>

    <delete id="delete" parameterType="JustificativaVO">
        DELETE FROM
          logbook19_justificativas
        WHERE
          flogbook19_id = #{id};
    </delete>

    <select id="select" resultMap="result" >
        SELECT * FROM 
			logbook19_justificativas
		LEFT JOIN
			logbook18_grupo_justificativas
		ON 
			logbook19_justificativas.flogbook18_id = logbook18_grupo_justificativas.flogbook18_id
		ORDER BY
			flogbook19_data_cadastro
		DESC
    </select>
    
    <select id="selectUnique" resultMap="result" parameterType="JustificativaVO" >
        SELECT * FROM 
			logbook19_justificativas
		LEFT JOIN
			logbook18_grupo_justificativas
		ON 
			logbook19_justificativas.flogbook18_id = logbook18_grupo_justificativas.flogbook18_id
		WHERE
			flogbook19_id = #{id}
    </select>


    <resultMap id="result" type="JustificativaVO">
    	<id property="id" column="flogbook19_id" />
        <result property="descricao" column="flogbook19_descricao" />
        <result property="responsavel" column="flogbook19_responsavel" />
        <result property="dataCadastro" column="flogbook19_data_cadastro" />
        <association property="grupo" javaType="GrupoJustificativaVO">
           	<id property="id" column="flogbook18_id" />
        	<result property="nome" column="flogbook18_nome" />
        </association>
    </resultMap>

</mapper>