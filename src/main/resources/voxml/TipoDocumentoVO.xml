<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tipodocumento">

    <select id="select" resultMap="result" >
        SELECT
          *
        FROM
          logbook09_tipo_documento
        ORDER BY
          logbook09_tipo_documento.flogbook09_id
        DESC;
    </select>

    <resultMap id="result" type="TipoDocumentoVO">
    	<id property="id" column="flogbook09_id"/>
        <result property="descricao" column="flogbook09_descricao" />
    </resultMap>

</mapper>